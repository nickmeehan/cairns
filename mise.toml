# Pinned quality-gate tools — bin/setup runs `mise install`, bin/ci refuses
# to run without them. Bump versions here, never install ad hoc.
[tools]
xcodegen = "2.44.1"
swiftformat = "0.55.5"
swiftlint = "0.58.2"
xcbeautify = "2.28.0"
