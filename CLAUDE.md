# CLAUDE.md

@AGENTS.md

- The shared, tool-agnostic rules live in AGENTS.md (imported above).
- "Done" means `bin/ci` is green on this machine. Run it before yielding.
- `docs/decisions.md` records why things are the way they are — read it
  before "fixing" something that looks odd.
